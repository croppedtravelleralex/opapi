# PLAN.md

## 当前统一计划

当前这个目录的主任务，已经不是继续堆方案文档，而是：

> **把当前 Rust `api` 目录收口成“账号池 / 路由池 / 网关池”，并把注册机正式设定为外部供给侧项目 `auto_reg`。**

---

## 当前阶段目标

1. 稳定当前 Rust 网关与账号池主线
2. 明确 `auto_reg -> Rust api` 的接入边界
3. 完成注册机产物导入、状态回写、基础可观测能力
4. 为后续指纹浏览器 API 接入预留执行器抽象层

---

## 当前优先级

### P0
1. 将规划中的注册机正式设定为 `auto_reg`
2. 收口 `auto_reg` 与 Rust `api` 的字段映射与导入协议
3. 统一配置导入边界（邮箱 / 验证码 / 代理 / 平台 / 浏览器）
4. 设计指纹浏览器执行器抽象层

### P1
5. 让导入账号逐步参与实际路由
6. 继续补账号状态与健康状态模型
7. 规划注册结果回传与错误回写协议

---

## 当前判断

当前项目已经不再是“方案孵化目录”，而是：

- **Rust `api`**：账号池 / 路由池 / OpenAI 兼容网关
- **`auto_reg`**：注册机 / 账号供给侧 / 任务与平台执行侧

因此近期最重要的不是再造一个新注册机，而是：

- 把 `auto_reg` 正式纳入总方案
- 把两边的接入协议收干净
- 把配置模型和执行器抽象定清楚
- 为后续指纹浏览器接入减少返工
