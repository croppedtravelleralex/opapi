- 用户新增偏好：以后定时刷新/轮询类推进默认静默，不发送过程消息；仅在有实质结果、需要决策、发现风险/bug、或一轮任务完成时主动汇报。
- 已记录本次 OpenClaw 无响应问题与处理：根因是网关配置中的 bind 设为 loopback，仅监听本地回环地址，导致外部网络无法访问网关服务、各渠道无响应；受 tailscale 模式为 serve 限制，无法直接改成 lan 绑定。处理方式为重启 OpenClaw 服务以应用当前配置；恢复后服务运行正常，PID 为 1534146，网关监听端口为 12657（主端口）和 12659。
