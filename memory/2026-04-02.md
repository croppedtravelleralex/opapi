- 2026-04-02：本轮围绕模型链路与供应商做了大量排查与实测。用户强烈偏好“配置名必须体现真实供应商/真实模型”，不喜欢 `custom-api`、`*-main` 这类模糊名字；后续涉及 provider / alias 命名时，默认应使用可识别的真实来源名称。
- 2026-04-02：Date-Now 新 key `sk-8pF1FYGv1rl3Wq6zqycarOZgwi9vNAi8KEja2SWIMpTBSCBe` 直连 `https://api.date-now.uk/v1` 已验证可用；旧 key 曾出现额度不足。用户当前认可将 `date-now/gpt-5.4` 作为主力候选。
- 2026-04-02：ASXS 直连 `https://api.asxs.top/v1` 已探测到多种 GPT 系列模型可用；用户对不同供应商做了对比测试，并关注真实模型可用性而不是抽象别名。
- 2026-04-02：本轮综合测评结论中，`date-now/gpt-5.4` 被判断为当前最稳且质量最好的主力候选，`iflow/qwen3-max` 适合作为备用快链；`asxs/gpt-5.4` 可用但偏慢；`cliproxy/*`、部分 `jnm/*`、`neoverse/*` 当前存在失败/限流/403 等问题，应视作待修链路。
- 2026-04-02：用户明确要求“我要 /models 里能看到模型并且可以选择来用”，尤其在 Date-Now 接入场景下，希望模型不仅能在 OpenClaw 配置里使用，也要能在模型枚举/选择入口中可见；如果做不到，需要明确区分是 OpenClaw provider 还是 CLIProxyAPI /models，不要混淆。
- 2026-04-02：排查服务器基础环境时，确认本机为 Ubuntu 24.04.4 LTS，主机名 `VM-0-7-ubuntu`，公网 IP `43.156.233.219`，当前用户为 root；80 未监听，443 被 `tailscaled` 占用；本机未装 ufw/nginx/caddy，但已装 Docker，可运行 systemd 常驻服务。
- 2026-04-02：OpenClaw Gateway 当前是 `ws://127.0.0.1:18789`，`bind=loopback`，不是 OpenAI 兼容 HTTP `/v1/models` 服务；若用户未来要求 HTTP OpenAI 兼容入口，应考虑桥接层或继续使用 CLIProxyAPI，而不是误把 Gateway 当成原生 OpenAI HTTP API。
- 2026-04-02：安全审计中存在一个关键高危项：`tools.elevated.allowFrom.webchat = "*"`。后续如果继续做公网/网关暴露或远程入口整理，应优先收敛该权限配置，改为白名单或关闭。
- 2026-04-02：用户要求“以后这种事情不需要我动手”，偏好助手主动完成从排查、修复到验证的全流程，不喜欢只给命令让其自己执行；后续遇到环境配置/服务修复/链路打通，默认应尽量直接处理并给结果。
- 2026-04-02：围绕 `OpenClaw OpenAI Gateway Control Plane` 项目，已完成一版较完整的 `PRD v3` 与 `V1 Implementation Plan` 文档，内容覆盖：OpenAI 兼容入口、多 Provider（api/gateway/web/local）统一调度、账号池、模型动态分配、WebSession / BrowserProfile、Egress/IP 安全、治理、自动化、运营、路线图、风险清单、验收标准，并已写入工作区根目录文档文件。
- 2026-04-02：用户在项目推进上要求“步子迈大一点，一次完成的任务多一点”，不希望一直停留在碎片化汇报或过度展开的设计说明；后续推进此项目时，应优先采用“大包推进 + 完成后统一汇报”的节奏。
- 2026-04-02：已在工作区新建工程目录 `openclaw-openai-gateway/`，并落下第一批 Rust V1 工程骨架：`Cargo.toml`、`.env.example`、`src/main.rs`、`src/app.rs`、`src/config.rs`、`src/error.rs`、`src/state.rs`、`src/routes/{health,models}.rs`、`src/middleware/{auth,request_id}.rs`、`src/bridge/client.rs`、`README.md`。
- 2026-04-02：`openclaw-openai-gateway/` 已落下首批 SQLite migrations：`0001_identity.sql`、`0002_provider_and_models.sql`、`0003_resource_pools.sql`、`0004_routing_and_execution.sql`、`0005_health_and_state.sql`，工程已从“纯文档阶段”进入“代码骨架阶段”。
- 2026-04-02：编译 `openclaw-openai-gateway` 第一轮真实报错已出现并被定位为 Axum route handler 返回类型可见性问题（至少涉及 `/v1/models` 路由返回类型私有），说明项目已进入真实编译调试阶段；下一步主线应优先继续“编译成功 -> 启动服务 -> 验证 `healthz/readyz/models` -> 再补 chat/responses 最小实现”。
- 2026-04-02：用户指定 `opapi` 项目的 GitHub 仓库为 `git@github.com:croppedtravelleralex/opapi.git`（HTTPS 形式：`https://github.com/croppedtravelleralex/opapi`），后续该项目默认 push 到这个仓库。
- 2026-04-02：用户要求项目汇报采用固定模板并长期默认使用：先列 **3 项已实现的大功能**，再写“正在实现的大功能/小功能 + 主要能做什么 + 进度%”，再写“距离真正实现某大功能还缺哪些小功能及其目的”，再写“当前实现最终目标 CCC 的总进度”，最后给出 **4 组下一步功能组合**，其中 **第 1 组必须是最推荐方案并写明原因**。
- 2026-04-02：用户补充了项目汇报的版式偏好：整体仍应紧凑，但要保留适量空行；汇报中应明确区分 **AAA 大功能 / bbb 小功能 / CCC 最终目标**，避免重复表述“正在实现的功能是”导致阅读混乱。
- 2026-04-02：在 `/root/SelfMadeprojects/api`（对应 GitHub `opapi`）中，已把 `lightpanda-automation` 的项目规则/习惯迁移过去，补齐了 `AI.md`、`README.md`、`PLAN.md`、`FEATURES.md`、`CURRENT_DIRECTION.md`、`CURRENT_TASK.md`、`STATUS.md`、`PROGRESS.md`、`TODO.md` 等项目骨架文档，并初始化为 Rust 网关项目。
- 2026-04-02：`opapi` 当前已完成一批真实工程落地：最小 Rust API 骨架、`/healthz` / `/readyz` / `/v1/models` / `/v1/chat/completions`、单上游 OpenAI 兼容透传、最小 Bearer 鉴权、统一错误模型第一版、Dockerfile / docker-compose / smoke 脚本、mock upstream 验证工具、静态多上游路由结构与本地双上游联调。
- 2026-04-02：`opapi` 真实联调过程中踩到过“旧残留进程/旧配置干扰”的坑，第一次请求曾误打到旧的假 key 上游；后续排查后确认静态多上游模型映射可用：`gpt-5.4` 可命中 mock-a，`qwen3-max` 可命中 mock-b。以后做联调时，应优先排除旧进程、旧端口、旧 `.env` 残留造成的假结果。
- 2026-04-02：`opapi` 已支持从 `GATEWAY_API_KEYS` 和 `GATEWAY_API_KEYS_FILE` 读取网关 API Key；另新增 `keys.example.txt`、`scripts/mock_upstream.py`、增强版 `scripts/smoke.sh`。当前离“真实第三方多上游联调完成”仍差 **第 2 个已确认可用的真实上游**；本机当前已明确确认可用的真实上游只有本地 `cliproxy` 上的 `date-now-gpt-5.4`。
- 2026-04-02：用户新增一条硬性执行规则：当用户只发 **`1`** 时，默认表示“直接执行当前最推荐方案”；在结果或明确问题出来之前，不要多说一句废话，不要先解释、先复述或先铺垫。后续应把 `1` 视为项目推进快捷指令。
- 2026-04-02：用户要求项目汇报模板进一步升级为 **“项目状态面板”** 风格，核心区块为：`已实现能力 / 当前实现中 / 总盘子 / 阶段位置 / 评分 / 推荐方案 / 结论`。后续项目汇报默认优先采用该模板，而不是旧版“已实现 / 正在实现 / 距离实现 / 下一步组合”模板。
- 2026-04-02：用户曾要求“分 3 段发出来，充分利用流式传输功能”，但本会话表面表现为单回合只能稳定发送 1 条消息。后续不应把这一平台限制甩给用户；应由助手自行适配：若可连续发多条则分段发，若不可则自动合并成一条完整面板消息，不再让用户介入处理这个限制。
- 2026-04-02：`opapi` 的真实双上游联调阶段已确认：本地 `cliproxy` 上存在第 2 个真实可用上游 **`iflow / qwen3-max`**，且 `/models` 与 `/chat/completions` 都已实测通；通过 `opapi` 的联调结果显示 **`qwen3-max` 已经打通**，而 `date-now / gpt-5.4` 仍卡在最后一层返回体解析兼容问题（`upstream_decode_error` / `stream_decode_error`），说明请求已命中真实上游，但响应读取方式仍需修正。
- 2026-04-02：用户要求重新回到更大的产品主线：把当前项目重新定义为 **“多上游 AI 能力接入与自动化运营平台”**，而不是只当作“兼容 OpenAI 的入口 / 桥接 OpenClaw 的网关 / 账号模型出口调度器”。用户明确希望把文档重新完善到覆盖 **接入层、调度层、自动化层、安全层、治理层、观测层、运营层、演进层** 八大维度。
- 2026-04-02：用户给出了一版很完整的最终蓝图扩展方向，要求后续正式文档吸收进去，重点包括：账号自动化、模型自动化、出口/代理自动化、浏览器/Web Provider 自动化、策略自动化、安全（访问/管理面/凭证/执行/行为风险/配置）、治理（策略/组织/合规/变更）、观测与可解释性、运营、开发者体验、智能化、未来扩展、多协议、多执行器等模块。
- 2026-04-02：用户认可的新版总产品判断是：该应用的终局应是 **`AI Gateway + Routing Brain + Provider Control Plane + Security Layer + Ops Platform`**，中文可概括为“**AI 能力调度中枢**”；长期主干建议固定为 6 根主梁：`Provider 抽象 / Account-Session-Pool 抽象 / Model+Routing 抽象 / Egress-IP 抽象 / Security-Governance 抽象 / Automation-Ops 抽象`。
- 2026-04-02：用户当前希望优先做的文档收口方向已明确：把已有 `OpenClaw OpenAI Gateway Control Plane PRD v3` 与 `V1 Implementation Plan` 按上述大蓝图重新重构，重点补齐 **增强版最终功能矩阵、按自动化/安全/治理/运营四条主线拆阶段、风险清单与对应控制措施、PRD v3 / 架构蓝图 v3 文稿主干**。
