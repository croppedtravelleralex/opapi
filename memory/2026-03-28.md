# 2026-03-28

- 延续推进 `/root/SelfMadeprojects/dashboard` 项目，核心目标从“静态展示”进一步收口到“可触发真实采集 + 可展示真实运行历史”的账号池监控面板。
- 已确认并反复验证：页面上的“刷新显示”只负责重新读取当前静态 JSON，不负责重新采集；用户对此行为有明确感知，要求语义和真实能力一致。
- 已新增并跑通本机真实采集触发器：`scripts/collector_trigger_server.py`，监听 `127.0.0.1:18765`，可通过 token 保护的 `POST /collect-now` 触发真实链路。
- 已新增一键采集链路脚本：`scripts/run-collector-once.sh`，串起账号解析、snapshot 抓取、usage JSON 生成和 Pages 部署。
- 已通过 Tailscale/Funnel 将触发器挂到 HTTPS 路径：`https://vm-0-7-ubuntu.tail37a2af.ts.net/dashboard-trigger`，并验证公网 HTTPS 可以成功触发真实采集流程。
- 已为 Cloudflare Pages 接入安全代理层：`functions/api/collect-now.js` 与 `functions/api/collect-status.js`，目标是让前端按钮不直接暴露 trigger token。
- 用户多次指出一个关键真实问题：点击“采集”和“刷新”后，最近采集记录没有立即新增；经定位，根因之一是 `recent_runs` 之前是静态占位，后续又存在“最新 run 历史慢一拍”的时序问题。
- 该时序问题已修复：现在 trigger 会先写入 `logs/collector-trigger-runs.json`，再重新生成 `collector/openai-usage.latest.json`，使 `recent_runs` 能立即包含最新一次采集记录。
- 当前真实 recent runs 已能包含多条真实记录，例如：`collector-run-20260327145027`、`collector-run-20260327144913`、`collector-run-20260327144737`。
- 已新增 `account_change_summary`，从只看 summary 变化，升级为支持账号级变化摘要；当前最近一次结果里 `changed_count = 0`，表示账号级字段对比无变化，而不是功能失效。
- 已多次部署 Pages Functions 与前端更新，最近涉及提交包括：`965ffe1`（recent runs 对齐最新采集）、`dee614d`（账号级 diff 摘要）、`1fd2afb`（更细的账号级 diff 字段展示）。
- 当前线上“立即采集”仍未完全闭环的主要阻塞，不在前端代码，而在 Cloudflare Pages secret 配置：需要在 `alexstudio-web` 项目里补 `COLLECT_TRIGGER_URL` 与 `COLLECT_TRIGGER_TOKEN`。
- 曾尝试用 `wrangler pages secret put` 直接写入 Cloudflare Pages secret，但连续遇到 Cloudflare API `504 Gateway Timeout`；因此改为引导用户在 Cloudflare 中文后台手动填写。
- 已明确告知用户在 Cloudflare 中文后台（Pages → `alexstudio-web` → 设置 → 变量和机密）填写两个值：
  - `COLLECT_TRIGGER_URL = https://vm-0-7-ubuntu.tail37a2af.ts.net/dashboard-trigger`
  - `COLLECT_TRIGGER_TOKEN = /root/SelfMadeprojects/dashboard/private/collect-trigger.token` 文件中的完整内容
- 用户当前最关注的体验问题是：按钮点击后必须体现“立即新增 recent runs / 立即有采集结果反馈”，而不是只更新文案或重新读取旧 JSON。
- 用户新增明确偏好：在给出编号选项后，用户若直接回复数字（如“1”“2”“1234”），应默认按对应选项直接执行，不再重复确认。
- 用户特别提醒：推进项目时，完成实际改动后要记得同步更新 `PROGRESS.md`。
- 本轮已确认并修复：OpenClaw 的 `runtime=subagent` 现在可再次成功启动一次性子任务（`mode=run` 可用），但 `mode=session` 在当前聊天目标下仍不稳定/不可作为默认路径。
- 当前对子 agent 的稳定使用策略已明确：默认优先使用 `runtime=subagent + mode=run`，并要求结构化输出；不要默认依赖持久 `session` 模式。
- `lightpanda-automation` 项目本轮已连续推进并提交：`LightpandaRunner` 错误分类与 timeout kill 收口、running cancel 一致性收口、`scripts/smoke_test.sh`、`limit+offset` 分页、`scripts/lightpanda_verify.sh` 入口、文档同步更新。
- 新增工作约定文件：`SUBAGENT_TEMPLATE.md`，用于规范以后所有子 agent 的默认调用方式。核心策略：默认使用 `runtime=subagent + mode=run + sandbox=inherit + cleanup=delete + thinking=medium`，并强制结构化输出，不默认使用 `session` 模式。
- `SUBAGENT_TEMPLATE.md` 已补充“超短实战版”，用于以后快速套用子 agent 调用模板。用户偏好这种能直接落地使用的简化版模板。
