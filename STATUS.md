# STATUS.md

## 当前状态摘要

- **状态：** 已进入 **注册机接入边界明确 + 账号池资源入口打通阶段**
- **日期：** 2026-04-06
- **当前焦点：** 先把 Rust `api` 收口为稳定的账号池 / 路由池 / OpenAI 兼容网关，同时把 `auto_reg` 正式作为注册机供给侧接入进来，再推进配置映射与指纹浏览器执行器抽象

## 当前状态

当前目录已经具备：

1. 项目统一入口文档骨架
2. Rust 最小 API 骨架（`/healthz`、`/readyz`、`/v1/models`、`/v1/chat/completions`、`/v1/accounts`）
3. `POST /v1/chat/completions` 已支持单上游透传与静态多上游映射
4. `/v1/*` 已接入最小 Bearer 鉴权，并已完成真实 401/200 验证
5. 多上游 `/v1` 语义已统一，并已补 smoke 验证脚本 `scripts/smoke_multi_upstream.sh`
6. `/v1/models` 已支持返回模型对应的上游、目标端点和基础状态视图
7. 已接入本地 SQLite，并落下最小账号池 store
8. 已提供 `/v1/accounts` 只读接口
9. 已提供 `POST /v1/accounts/import` 与 `POST /v1/accounts/status`，可接入 `auto_reg` 等注册机产出
10. 已补 `PLAN.md`、`CURRENT_DIRECTION.md`、`FEATURES.md`、`INTEGRATION_PLAN.md`，并正式确认 **注册机 = `auto_reg`，Rust `api` = 账号池 / 路由池 / 网关**

当前目录还不具备：

1. 导入账号已正式参与真实路由选择
2. 指纹浏览器执行器抽象层代码
3. 配置映射表与接入适配器代码
4. 完整健康检查 / fallback / 动态调度系统

## 当前风险

1. `STATUS.md` / `TODO.md` / `PROGRESS.md` 此前一度落后于真实代码状态，后续若不持续同步，容易再次误导推进
2. `data/gateway.db` 当前已进入仓库，运行态数据与代码边界还不够干净
3. `target/` 构建目录体积很大，当前磁盘开销主要来自 Rust 构建缓存，而不是源码本身
4. 当前双项目结构（Rust `api` + `auto_reg`）已成形，后续主要风险从“能不能做”转为“边界是否收干净”

## 当前下一步

### P0
1. 同步清理仓库卫生与状态文档
2. 设计指纹浏览器执行器抽象层
3. 收口配置映射边界（邮箱 / 验证码 / 代理 / 平台 / 浏览器）
4. 逐步让导入账号参与实际路由

## 本轮体检（2026-04-06）

- **找 bug：** 当前最明显的问题已不再是网关主链 bug，而是文档状态落后与运行态数据边界不干净。
- **性能评分：** 当前阶段 **8.7/10**。运行链路轻、SQLite 开销低、`cargo check` 可通过；主要开销集中在 `target/` 构建缓存与双项目协同带来的认知成本。
- **改进建议：** 下一步最值动作是 **先把文档和仓库卫生收口，再进入指纹浏览器执行器抽象设计**。
