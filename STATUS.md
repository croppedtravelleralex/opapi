# STATUS.md

## 当前状态摘要

- **状态：** 已进入 **最小可用网关收口 + 多上游兼容验证阶段**
- **日期：** 2026-04-06
- **当前焦点：** 先把 health / models / chat / 鉴权 / 多上游路径语义这一条最小可用链路彻底跑通，再往账号池和治理能力推进

## 当前状态

当前目录已经具备：

1. 两份原始方案文档
2. 项目统一入口文档骨架
3. 当前方向 / 当前任务 / 状态 / 待办 / 进展的最小文档结构
4. Rust 最小 API 骨架（`/healthz`、`/readyz`、`/v1/models`、`/v1/chat/completions`）
5. 最小环境变量样例（`.env.example`）
6. 配置设计文档 `CONFIG.md`
7. 数据模型设计文档 `DATA_MODEL.md`
8. `.gitignore` 已补
9. `POST /v1/chat/completions` 已支持单上游 OpenAI 兼容接口透传
10. `/v1/*` 已接入最小 Bearer 鉴权设计，并已完成一轮真实 401/200 验证
11. 已补多上游路由演进文档 `ROUTING_PLAN.md`
12. 已验证一条 chat 真实透传链路可打通
13. 已补静态多上游 smoke 验证脚本雏形

当前目录还不具备：

1. 已稳定通过的双上游 smoke 结果
2. 完整的多上游 URL 语义收口
3. 账号池数据落库
4. 更真实的 models 路由视图

## 当前风险

1. `/v1` 路径拼接语义此前在单上游与多上游上存在不一致，容易造成部分上游 404
2. 文档状态一度落后于真实代码和验证结果，容易误导后续推进
3. 当前仍处于最小闭环阶段，离真正账号池网关还有明显距离

## 当前下一步

### P0
1. 修正并统一多上游 `/v1` 路径拼接策略
2. 跑通双上游 smoke 验证
3. 收口当前文档状态并保持与代码同步
4. 整理当前未提交改动并完成一轮 Git 提交

## 本轮体检（2026-04-06）

- **找 bug：** 已确认多上游 URL 拼接策略是当前最关键真实 bug，表现为部分上游可能请求到 `/chat/completions` 而不是 `/v1/chat/completions`。
- **性能评分：** 当前阶段 **8.1/10**。代码结构、启动速度和单链路验证都不错，但多上游稳定性还没完全收口。
- **改进建议：** 下一步最值动作是 **先统一 `/v1` 语义并跑通双上游 smoke**。
