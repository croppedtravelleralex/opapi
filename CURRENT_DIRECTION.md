# CURRENT_DIRECTION.md

## 当前方向

当前最高优先级目标，已经不是继续扩展设想，也不是自造新的注册机，而是：

1. **把注册机正式设定为 `auto_reg`**
2. **把当前 Rust `api` 明确收口为账号池 / 路由池 / 网关池**
3. **把两边的配置导入、结果回传、状态回写边界定清楚**
4. **在不污染当前主线的前提下，为指纹浏览器 API 接入预留执行器抽象**

## 当前判断

当前阶段不是做大而全平台，而是：

> **完成“注册机 = `auto_reg`、网关 = Rust `api`”这一双项目结构的收口，再推进配置接入与指纹浏览器执行层。**
